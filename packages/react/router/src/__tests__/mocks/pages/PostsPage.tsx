export const handle = {
  route: {
    description: 'Posts list',
  },
};

/**
 * Mock posts list page used in router tests.
 *
 * @returns The posts page
 */
export default function PostsPage() {
  return <div>Posts Page</div>;
}
