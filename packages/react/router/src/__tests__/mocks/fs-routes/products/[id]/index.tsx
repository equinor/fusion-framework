export const handle = {
  route: {
    description: 'Product detail page',
  },
};

/**
 * Mock directory-based (fs-routing) product detail page used in router tests.
 *
 * @returns The product detail page
 */
export default function ProductsIdIndexPage() {
  return <div>Product</div>;
}
