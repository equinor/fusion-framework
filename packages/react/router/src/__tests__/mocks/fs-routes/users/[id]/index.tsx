export const handle = {
  route: {
    description: 'User detail page',
  },
};

/**
 * Mock directory-based (fs-routing) user detail page used in router tests.
 *
 * @returns The user detail page
 */
export default function UsersIdIndexPage() {
  return <div>User</div>;
}
