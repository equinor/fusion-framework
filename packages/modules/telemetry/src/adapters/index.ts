/**
 * Re-exports the base adapter class and interface for building custom telemetry adapters.
 *
 * @remarks
 * Import from `@equinor/fusion-framework-module-telemetry/adapter` to access
 * {@link BaseTelemetryAdapter} and {@link ITelemetryAdapter}.
 */
export {
  BaseTelemetryAdapter,
  type ITelemetryAdapter,
} from '../TelemetryAdapter.js';
