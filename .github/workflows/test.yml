name: Test project files

on: 
  pull_request:
    paths:
      - '**.ts'
      - '**.tsx'

jobs:
  jest:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2

      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Install Node Dependencies
        run: npm ci

      - name: Test Code
        uses: pomekhin/jest-github-action@1.1.5
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          changes-only: true
          min-coverage: 90