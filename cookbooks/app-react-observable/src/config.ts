import type { AppModuleInitiator } from '@equinor/fusion-framework-react-app';

/**
 * Keeps the observable cookbook app configuration intentionally empty.
 */
export const configure: AppModuleInitiator = (): void => undefined;

export default configure;
