import { TodoListManager } from '@local/components/Todo';

export const Todo = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <TodoListManager />
    </div>
  );
};

export default Todo;
