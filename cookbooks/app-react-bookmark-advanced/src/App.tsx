import { StrictMode } from 'react';
import Router from './Router';

export const App = () => {
    return (
        <StrictMode>
            <Router />
        </StrictMode>
    );
};

export default App;
