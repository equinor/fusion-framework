---
"@equinor/fusion-framework-react-module-event": patch
---

Internal: add intent comment to the `.pipe()` operator chain in `useEventStream.ts` to satisfy the `require-intent-comment/rxjs` lint rule. No functional change.
