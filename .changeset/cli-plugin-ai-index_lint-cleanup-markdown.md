---
"@equinor/fusion-framework-cli-plugin-ai-index": patch
---

Internal: add clarifying intent comments in `zod-to-azure-fields`, and split `utils/markdown/parser.ts` into single-export `parse-markdown.ts` and `parse-markdown-file.ts` modules re-exported from the existing barrel; no public API or behavior changes.
