---
"@equinor/fusion-observable": patch
---

Internal: Consolidate the nine single-export action helper files under `src/actions/` into `utils.ts` (type/suffix helpers) and `predicates.ts` (lifecycle predicates) to reduce file fragmentation. No change to the public API.
