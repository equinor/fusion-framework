---
"@equinor/fusion-framework-cli-plugin-ai-index": patch
---

Internal: add clarifying intent comments to control-flow, iterator, and RxJS pipeline logic in `apply-metadata`, `file-stream`, `execute-pipeline`, `embedding-dimensions`, `package-resolver`, and `get-diff`; no public API or behavior changes.
