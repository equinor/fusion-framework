---
"@equinor/fusion-framework-vitest-plugin-react-app": patch
---

Document `test.override(...)` for overriding a fixture (`configure`, `fusion`, etc.) within a
single test file or `describe` block, alongside the existing `test.extend(...)` guide, in the
package's "Advanced usage" doc (`docs/advanced.md`).
