---
"@equinor/fusion-framework-module-app": patch
---

Internal: resolve `noExplicitAny` Biome warnings in `enableAppModule`'s configurator widening, `AppBundleState`/`AppBundleStateInitial`'s `TModules` generic default, and the `setModule` action creator, using explanatory `biome-ignore` comments. No public API or behavior change.
