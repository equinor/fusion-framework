---
"@equinor/fusion-framework-react-components-people-provider": patch
---

Internal: add intent comments to `.pipe()` operator chains in `PersonController.ts` and `create-resolver.ts`, replacing several non-functional placeholder comments with meaningful ones, to satisfy the `require-intent-comment/rxjs` lint rule. No functional change.
