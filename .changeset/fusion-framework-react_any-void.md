---
"@equinor/fusion-framework-react": patch
---

Internal: resolve `noExplicitAny` Biome warnings in the framework React context, `createFrameworkProvider`'s `TRef` generic default, and `useCurrentAppModule`'s dynamic module-key lookup, using explanatory `biome-ignore` comments. No public API or behavior change.
