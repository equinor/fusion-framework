---
"@equinor/fusion-log": patch
---

Internal: Add intent comments for switch/if control flow and iterator/pipe usage, remove a stale TODO, and suppress `single-export-per-file` for the deliberately co-located `LogLevel` enum and `defaultLogLevel` constant, resolving all `fusion-lint` warnings in this package.
