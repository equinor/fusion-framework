---
"@equinor/fusion-framework-react": patch
---

Internal: add intent comments to `.pipe()` operator chains in `useFeature.ts` and `useFeatures.ts` to satisfy the `require-intent-comment/rxjs` lint rule. No functional change.
