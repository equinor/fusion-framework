---
"@equinor/fusion-framework-cli-plugin-copilot": patch
---

Internal: bump `@github/copilot-sdk` from `1.0.9` to `1.0.11` and adapt `firstStringProp` to the SDK's re-typed `ToolExecutionStartData.arguments` (now `JsonValue` instead of a plain index-signature object).
