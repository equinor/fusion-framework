---
"@equinor/fusion-observable": patch
---

Internal: split `useObservableInputState` and `useObservableLayoutSubscription` into their own modules and added missing intent comments and TSDoc; no public API changes.
