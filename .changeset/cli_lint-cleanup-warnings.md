---
"@equinor/fusion-framework-cli": patch
---

Internal: added missing intent comments ahead of control-flow blocks, loops, and array iterator calls (`.map`/`.filter`) across the CLI source to satisfy `fusion-lint`'s `require-intent-comment` rules. No behavior changes.
