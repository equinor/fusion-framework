---
"@equinor/fusion-framework-cli-plugin-ai-index": patch
---

Internal: add clarifying intent comments and split `utils/git/file-changes.ts` into single-export `get-changed-files.ts`, `get-file-status.ts`, and `is-file-changed.ts` modules, re-exported from the existing barrel; no public API or behavior changes.
