---
"@equinor/fusion-framework-cli-plugin-ai-base": patch
---

Internal: Consolidate the eight single-export CLI option files under `src/options/` back into `options.ts` to reduce file fragmentation. No change to the public API or option behavior.
