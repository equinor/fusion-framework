---
"@equinor/fusion-framework-cookbook-app-react-bookmark-advanced": patch
---

Internal: resolve `fusion-lint` warnings (`filename-convention`, `require-intent-comment`, `require-tsdoc`). Renamed `Routes.tsx` to `routes.tsx` and added missing intent comments/TSDoc across `init.ts`, `types.ts`, and `useBookmarkContext.ts`. No behavior change.
