---
"@equinor/fusion-framework-react-app": patch
---

Internal: add intent comments to control-flow statements (`if`/`switch`/`.map()`) across several files to satisfy the `require-intent-comment/flow` and `require-intent-comment/iterators` lint rules. No functional change.
