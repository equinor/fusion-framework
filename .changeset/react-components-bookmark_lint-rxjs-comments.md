---
"@equinor/fusion-framework-react-components-bookmark": patch
---

Internal: add intent comments to `.pipe()` operator chains in `Bookmark.tsx` and `ImportBookmark.tsx` to satisfy the `require-intent-comment/rxjs` lint rule. No functional change.
