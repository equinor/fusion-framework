---
"@equinor/fusion-framework-vite-plugin-api-service": patch
---

Clarify that Fusion application service mocks should use executable `<service>.mock.ts` modules,
while API plugin routes are low-level hooks for server-owned development behavior.
