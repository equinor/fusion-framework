---
"@equinor/fusion-framework-module-feature-flag": patch
---

Use the supported navigation `history` API in the URL feature-flag plugin, avoiding deprecated
`navigator` telemetry warnings when applications connect the plugin.