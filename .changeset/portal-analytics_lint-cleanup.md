---
"portal-analytics": patch
---

Internal: resolve `fusion-lint` warnings (`single-export-per-file`). Split `schema.ts` into dedicated `log-entry-schema.ts` and `log-record-schema.ts` modules, and updated `useAppContextNavigation.ts` accordingly. No behavior change.
