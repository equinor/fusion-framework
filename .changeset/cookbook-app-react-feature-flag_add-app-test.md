---
"@equinor/fusion-framework-cookbook-app-react-feature-flag": patch
---

Internal: add Vitest coverage for the `FeatureFlag`/`FeatureFlags` components (moved into `src/components/`, with `useFeatureLogger` moved into `src/hooks/`) and wire the cookbook into the root Vitest project list; no runtime changes.
