---
"@equinor/fusion-framework-cookbook-app-react-charts": patch
---

Internal: resolve `fusion-lint` warnings (`single-export-per-file`, `require-intent-comment`). Extracted chart data into dedicated `market-share-data.ts`, `sales-data.ts`, and `temperature-data.ts` modules and split styling into `styled.ts`. No behavior change.
