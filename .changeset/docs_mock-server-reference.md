---
"@equinor/fusion-framework-docs": patch
---

Add the OpenAPI mock server reference covering setup, layered service definitions, Playwright
overrides, reset behavior, and the `ffc app dev --mock` workflow. Also add missing cookbook pages
and navigation entries to the cookbook index and sidebar.
