---
"@equinor/fusion-framework-react-app": patch
---

Internal: add intent comments to `.pipe()` operator chains in `useAppEnvironmentVariables.ts`, `useAppSetting.ts`, and `useAppSettingsStatus.ts` to satisfy the `require-intent-comment/rxjs` lint rule. No functional change.
