---
"@equinor/fusion-framework-cli": patch
---

Internal: Consolidate single-export CLI auth option files under `src/cli/options/` into `options.ts`, and remove the unused `envOption` export left over from a prior file split. No change to the public API or CLI behavior.
