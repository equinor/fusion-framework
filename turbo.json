{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["GITHUB_ACTIONS"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["VUEPRESS_BASE"],
      "outputs": ["dist/**"],
      "cache": true
    }
  }
}
